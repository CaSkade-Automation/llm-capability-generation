There is a coffeemaking capability that receives coffee beans and water as input, which are both products. In addition the capability receives type of coffe (e.g., Americano, Espresso) as input, which is an information. The capability returns coffee and coffee grounds as output, wich are both products. The water has a property temperature, which is measured in degrees Celsius (°C) and must be >= 0 and <= 50 as requirements and is only determined when the capability is used. Type of coffee has a property that determines the desired type of coffee of type coffee type, which is only determined when the capability is used. In the same way, the output coffee has a property that gives the type of coffee as an assurance, which has to be equal to to the desired type of coffee from the input. In addition, the output coffee has a property temperature that must be >= 90 as an assurance. 