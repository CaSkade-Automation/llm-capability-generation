There is a addition capability that receives two integer numbers "a" and "b" as input and returns a integer sum "c" as output, where all three entities are information. Each of the two inputs has a property that determines the value of this number. In the same way, the output sum has a property that gives the result of the addition as an assurance. 